# Run Instructions

1. **Install dependencies and compile**:
   ```bash
   sbt compile
   ```

2. **Run the server**:
   ```bash
   sbt run
   ```
   The server will start on port 8080.
   The SQLite database `magnetar_scala.db` will be created automatically.

3. **Run tests**:
   ```bash
   sbt test
   ```
